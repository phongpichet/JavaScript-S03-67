// Example 1
let length = 6;
let width = 7;
let perimeter = 2 * (length + width);
console.log(`findPerimeter(${length},${width})=${perimeter}`); // Output: findPerimeter(6,7)=26

// Example 2
length = 20;
width = 10;
perimeter = 2 * (length + width);
console.log(`findPerimeter(${length},${width})=${perimeter}`); // Output: findPerimeter(20,10)=60

// Example 3
length = 2;
width = 9;
perimeter = 2 * (length + width);
console.log(`findPerimeter(${length},${width})=${perimeter}`); // Output: findPerimeter(2,9)=22
